generator client {
  provider   = "prisma-client-js"
  engineType = "binary"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model activity_logs {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id    BigInt?   @db.UnsignedBigInt
  method     String    @db.VarChar(10)
  url        String    @db.LongText
  action     String?   @db.VarChar(255)
  model_type String?   @db.VarChar(255)
  model_id   BigInt?   @db.UnsignedBigInt
  payload    String?   @db.LongText
  ip_address String?   @db.VarChar(45)
  user_agent String?   @db.Text
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)
  users      users?    @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "activity_logs_user_id_foreign")

  @@index([model_type, model_id], map: "activity_logs_model_type_model_id_index")
  @@index([user_id], map: "activity_logs_user_id_foreign")
}

model announcements {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  company_id BigInt    @db.UnsignedBigInt
  user_id    BigInt    @db.UnsignedBigInt
  title      String    @db.VarChar(255)
  status     Boolean   @default(true)
  content    String    @db.LongText
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model attachments {
  id             BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  owner_type     String    @db.VarChar(50)
  owner_id       BigInt    @db.UnsignedBigInt
  file_name      String?   @db.VarChar(255)
  file_path      String?   @db.VarChar(500)
  url            String?   @db.Text
  meta           Json?
  uploaded_by_id BigInt?   @db.UnsignedBigInt
  uploaded_at    DateTime  @default(now()) @db.Timestamp(0)
  created_at     DateTime? @db.Timestamp(0)
  updated_at     DateTime? @db.Timestamp(0)
  users          users?    @relation(fields: [uploaded_by_id], references: [id], onUpdate: NoAction, map: "attachments_uploaded_by_id_foreign")

  @@index([owner_type, owner_id], map: "attachments_owner_idx")
  @@index([uploaded_by_id], map: "attachments_uploaded_by_id_foreign")
}

model bug_reports {
  id         BigInt               @id @default(autoincrement()) @db.UnsignedBigInt
  company_id BigInt               @db.UnsignedBigInt
  user_id    BigInt               @db.UnsignedBigInt
  title      String               @db.VarChar(255)
  status     Boolean              @default(true)
  message    String               @db.LongText
  platform   bug_reports_platform @default(android)
  image      String               @db.VarChar(255)
  created_at DateTime?            @db.Timestamp(0)
  updated_at DateTime?            @db.Timestamp(0)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model cache {
  key        String @db.VarChar(255)
  value      String @db.MediumText
  expiration Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model cache_locks {
  key        String @db.VarChar(255)
  owner      String @db.VarChar(255)
  expiration Int

  @@ignore
}

model companies {
  id           BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  name         String    @db.VarChar(255)
  latitude     String    @db.VarChar(255)
  longitude    String    @db.VarChar(255)
  radius       String    @db.VarChar(255)
  full_address String    @db.VarChar(255)
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
  deleted_at   DateTime? @db.Timestamp(0)
}

model departements {
  id                                                        BigInt            @id @default(autoincrement()) @db.UnsignedBigInt
  company_id                                                BigInt            @db.UnsignedBigInt
  name                                                      String            @db.VarChar(255)
  created_at                                                DateTime?         @db.Timestamp(0)
  updated_at                                                DateTime?         @db.Timestamp(0)
  deleted_at                                                DateTime?         @db.Timestamp(0)
  design_requests                                           design_requests[]
  work_orders_work_orders_department_idTodepartements       work_orders[]     @relation("work_orders_department_idTodepartements")
  work_orders_work_orders_department_providesTodepartements work_orders[]     @relation("work_orders_department_providesTodepartements")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model design_approvals {
  id                BigInt                  @id @default(autoincrement()) @db.UnsignedBigInt
  design_request_id BigInt                  @db.UnsignedBigInt
  approver_id       BigInt?                 @db.UnsignedBigInt
  status            design_approvals_status @default(PENDING)
  decided_at        DateTime?               @db.Timestamp(0)
  remarks           String?                 @db.Text
  created_at        DateTime?               @db.Timestamp(0)
  updated_at        DateTime?               @db.Timestamp(0)
  users             users?                  @relation(fields: [approver_id], references: [id], onUpdate: NoAction, map: "design_approvals_approver_id_foreign")
  design_requests   design_requests         @relation(fields: [design_request_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "design_approvals_design_request_id_foreign")

  @@index([approver_id], map: "design_approvals_approver_id_foreign")
  @@index([design_request_id, status], map: "design_approvals_status_idx")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model design_request_items {
  id                BigInt          @id @default(autoincrement()) @db.UnsignedBigInt
  design_request_id BigInt          @db.UnsignedBigInt
  line_no           Int
  kebutuhan         String          @db.Text
  isi_konten        String?         @db.Text
  ukuran            String?         @db.VarChar(100)
  referensi         String?         @db.Text
  keterangan        String?         @db.Text
  created_at        DateTime?       @db.Timestamp(0)
  updated_at        DateTime?       @db.Timestamp(0)
  design_requests   design_requests @relation(fields: [design_request_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "design_request_items_design_request_id_foreign")

  @@unique([design_request_id, line_no], map: "dri_unique_line")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model design_requests {
  id                                              BigInt                   @id @default(autoincrement()) @db.UnsignedBigInt
  request_no                                      String?                  @unique(map: "design_requests_request_no_unique") @db.VarChar(50)
  request_date                                    DateTime                 @db.Date
  need_by_date                                    DateTime?                @db.Date
  priority                                        design_requests_priority
  pic_id                                          BigInt                   @db.UnsignedBigInt
  division_id                                     BigInt?                  @db.UnsignedBigInt
  submitted_to_id                                 BigInt?                  @db.UnsignedBigInt
  acknowledged_by_id                              BigInt?                  @db.UnsignedBigInt
  status                                          design_requests_status   @default(DRAFT)
  notes                                           String?                  @db.Text
  created_at                                      DateTime?                @db.Timestamp(0)
  updated_at                                      DateTime?                @db.Timestamp(0)
  design_approvals                                design_approvals[]
  design_request_items                            design_request_items[]
  users_design_requests_acknowledged_by_idTousers users?                   @relation("design_requests_acknowledged_by_idTousers", fields: [acknowledged_by_id], references: [id], onUpdate: NoAction, map: "design_requests_acknowledged_by_id_foreign")
  departements                                    departements?            @relation(fields: [division_id], references: [id], onUpdate: NoAction, map: "design_requests_division_id_foreign")
  users_design_requests_pic_idTousers             users                    @relation("design_requests_pic_idTousers", fields: [pic_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "design_requests_pic_id_foreign")
  users_design_requests_submitted_to_idTousers    users?                   @relation("design_requests_submitted_to_idTousers", fields: [submitted_to_id], references: [id], onUpdate: NoAction, map: "design_requests_submitted_to_id_foreign")

  @@index([acknowledged_by_id], map: "design_requests_acknowledged_by_id_foreign")
  @@index([division_id], map: "design_requests_division_id_foreign")
  @@index([pic_id], map: "design_requests_pic_id_foreign")
  @@index([submitted_to_id], map: "design_requests_submitted_to_id_foreign")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model documentations {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  title      String    @db.VarChar(255)
  subtitle   String?   @db.VarChar(255)
  text_docs  String    @db.LongText
  status     Boolean   @default(true)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

model exports {
  id              BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  completed_at    DateTime? @db.Timestamp(0)
  file_disk       String    @db.VarChar(255)
  file_name       String?   @db.VarChar(255)
  exporter        String    @db.VarChar(255)
  processed_rows  Int       @default(0) @db.UnsignedInt
  total_rows      Int       @db.UnsignedInt
  successful_rows Int       @default(0) @db.UnsignedInt
  user_id         BigInt    @db.UnsignedBigInt
  created_at      DateTime? @db.Timestamp(0)
  updated_at      DateTime? @db.Timestamp(0)
}

model failed_import_rows {
  id               BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  data             String    @db.LongText
  import_id        BigInt    @db.UnsignedBigInt
  validation_error String?   @db.Text
  created_at       DateTime? @db.Timestamp(0)
  updated_at       DateTime? @db.Timestamp(0)
}

model failed_jobs {
  id         BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid       String   @db.VarChar(255)
  connection String   @db.Text
  queue      String   @db.Text
  payload    String   @db.LongText
  exception  String   @db.LongText
  failed_at  DateTime @default(now()) @db.Timestamp(0)
}

model fcm_models {
  id           BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id      BigInt    @db.UnsignedBigInt
  device_token String    @db.LongText
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
}

model imports {
  id              BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  completed_at    DateTime? @db.Timestamp(0)
  file_name       String    @db.VarChar(255)
  file_path       String    @db.VarChar(255)
  importer        String    @db.VarChar(255)
  processed_rows  Int       @default(0) @db.UnsignedInt
  total_rows      Int       @db.UnsignedInt
  successful_rows Int       @default(0) @db.UnsignedInt
  user_id         BigInt    @db.UnsignedBigInt
  created_at      DateTime? @db.Timestamp(0)
  updated_at      DateTime? @db.Timestamp(0)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model job_batches {
  id             String  @db.VarChar(255)
  name           String  @db.VarChar(255)
  total_jobs     Int
  pending_jobs   Int
  failed_jobs    Int
  failed_job_ids String  @db.LongText
  options        String? @db.MediumText
  cancelled_at   Int?
  created_at     Int
  finished_at    Int?

  @@ignore
}

model job_levels {
  id             BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  company_id     BigInt    @db.UnsignedBigInt
  departement_id BigInt    @db.UnsignedBigInt
  name           String    @db.VarChar(255)
  created_at     DateTime? @db.Timestamp(0)
  updated_at     DateTime? @db.Timestamp(0)
  deleted_at     DateTime? @db.Timestamp(0)
}

model job_positions {
  id             BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  company_id     BigInt    @db.UnsignedBigInt
  departement_id BigInt    @db.UnsignedBigInt
  name           String    @db.VarChar(255)
  created_at     DateTime? @db.Timestamp(0)
  updated_at     DateTime? @db.Timestamp(0)
  deleted_at     DateTime? @db.Timestamp(0)
}

model jobs {
  id           BigInt @id @default(autoincrement()) @db.UnsignedBigInt
  queue        String @db.VarChar(255)
  payload      String @db.LongText
  attempts     Int    @db.UnsignedTinyInt
  reserved_at  Int?   @db.UnsignedInt
  available_at Int    @db.UnsignedInt
  created_at   Int    @db.UnsignedInt
}

model log_user_attendances {
  id         BigInt                     @id @default(autoincrement()) @db.UnsignedBigInt
  user_id    BigInt?                    @db.UnsignedBigInt
  type       log_user_attendances_type? @default(in)
  created_at DateTime?                  @db.Timestamp(0)
  updated_at DateTime?                  @db.Timestamp(0)
  users      users?                     @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "log_user_attendances_user_id_foreign")

  @@index([user_id], map: "log_user_attendances_user_id_foreign")
}

model migrations {
  id        Int    @id @default(autoincrement()) @db.UnsignedInt
  migration String @db.VarChar(255)
  batch     Int
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model model_has_permissions {
  permission_id BigInt @db.UnsignedBigInt
  model_type    String @db.VarChar(255)
  model_id      BigInt @db.UnsignedBigInt

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model model_has_roles {
  role_id    BigInt @db.UnsignedBigInt
  model_type String @default("App\\Models\\User") @db.VarChar(255)
  model_id   BigInt @db.UnsignedBigInt

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model notifications {
  id              String    @db.Char(36)
  type            String    @db.VarChar(255)
  notifiable_type String    @db.VarChar(255)
  notifiable_id   BigInt    @db.UnsignedBigInt
  data            String    @db.Text
  read_at         DateTime? @db.Timestamp(0)
  created_at      DateTime? @db.Timestamp(0)
  updated_at      DateTime? @db.Timestamp(0)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model password_reset_tokens {
  email      String    @db.VarChar(255)
  token      String    @db.VarChar(255)
  created_at DateTime? @db.Timestamp(0)

  @@ignore
}

model permissions {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  name       String    @db.VarChar(255)
  guard_name String    @db.VarChar(255)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

model permit_approves {
  id           BigInt                       @id @default(autoincrement()) @db.UnsignedBigInt
  permit_id    BigInt                       @db.UnsignedBigInt
  user_id      BigInt                       @db.UnsignedBigInt
  user_type    String                       @db.VarChar(255)
  user_approve permit_approves_user_approve @default(w)
  notes        String?                      @db.LongText
  created_at   DateTime?                    @db.Timestamp(0)
  updated_at   DateTime?                    @db.Timestamp(0)
}

model permit_types {
  id              BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  type            String    @db.VarChar(100)
  is_payed        Boolean   @default(true)
  approve_line    Boolean   @default(true)
  approve_manager Boolean   @default(true)
  approve_hr      Boolean   @default(true)
  with_file       Boolean   @default(false)
  show_mobile     Boolean   @default(false)
  created_at      DateTime? @db.Timestamp(0)
  updated_at      DateTime? @db.Timestamp(0)
  deleted_at      DateTime? @db.Timestamp(0)
}

model permits {
  id                        BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  permit_numbers            String    @db.VarChar(100)
  user_id                   BigInt    @db.UnsignedBigInt
  permit_type_id            BigInt    @db.UnsignedBigInt
  user_timework_schedule_id BigInt    @db.UnsignedBigInt
  timein_adjust             DateTime? @db.Time(0)
  timeout_adjust            DateTime? @db.Time(0)
  current_shift_id          BigInt?   @db.UnsignedBigInt
  adjust_shift_id           BigInt?   @db.UnsignedBigInt
  start_date                DateTime? @db.Date
  end_date                  DateTime? @db.Date
  start_time                DateTime? @db.Time(0)
  end_time                  DateTime? @db.Time(0)
  notes                     String?   @db.LongText
  file                      String?   @db.VarChar(255)
  created_at                DateTime? @db.Timestamp(0)
  updated_at                DateTime? @db.Timestamp(0)
  deleted_at                DateTime? @db.Timestamp(0)
}

model personal_access_tokens {
  id             BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  tokenable_type String    @db.VarChar(255)
  tokenable_id   BigInt    @db.UnsignedBigInt
  name           String    @db.VarChar(255)
  token          String    @db.VarChar(64)
  abilities      String?   @db.Text
  last_used_at   DateTime? @db.Timestamp(0)
  expires_at     DateTime? @db.Timestamp(0)
  created_at     DateTime? @db.Timestamp(0)
  updated_at     DateTime? @db.Timestamp(0)
}

model qr_presence_transactions {
  id                 BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  qr_presence_id     BigInt    @db.UnsignedBigInt
  user_attendance_id BigInt    @db.UnsignedBigInt
  token              String    @db.VarChar(255)
  created_at         DateTime? @db.Timestamp(0)
  updated_at         DateTime? @db.Timestamp(0)
}

model qr_presences {
  id             BigInt            @id @default(autoincrement()) @db.UnsignedBigInt
  type           qr_presences_type @default(in)
  departement_id BigInt            @db.UnsignedBigInt
  timework_id    BigInt            @db.UnsignedBigInt
  token          String            @db.VarChar(255)
  for_presence   DateTime          @default(now()) @db.DateTime(0)
  expires_at     DateTime          @default(now()) @db.DateTime(0)
  created_at     DateTime?         @db.Timestamp(0)
  updated_at     DateTime?         @db.Timestamp(0)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model role_has_permissions {
  permission_id BigInt @db.UnsignedBigInt
  role_id       BigInt @db.UnsignedBigInt

  @@ignore
}

model roles {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  name       String    @db.VarChar(255)
  guard_name String    @db.VarChar(255)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model sessions {
  id            String  @db.VarChar(255)
  user_id       BigInt? @db.UnsignedBigInt
  ip_address    String? @db.VarChar(45)
  user_agent    String? @db.Text
  payload       String  @db.LongText
  last_activity Int

  @@ignore
}

model settings {
  id                           BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  company_id                   BigInt    @db.UnsignedBigInt
  attendance_image_geolocation Boolean   @default(false)
  attendance_qrcode            Boolean   @default(false)
  attendance_fingerprint       Boolean   @default(false)
  created_at                   DateTime? @db.Timestamp(0)
  updated_at                   DateTime? @db.Timestamp(0)
  deleted_at                   DateTime? @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model status_histories {
  id            BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  owner_type    String    @db.VarChar(50)
  owner_id      BigInt    @db.UnsignedBigInt
  from_status   String?   @db.VarChar(30)
  to_status     String    @db.VarChar(30)
  changed_by_id BigInt?   @db.UnsignedBigInt
  changed_at    DateTime  @default(now()) @db.Timestamp(0)
  remarks       String?   @db.Text
  created_at    DateTime? @db.Timestamp(0)
  updated_at    DateTime? @db.Timestamp(0)
  users         users?    @relation(fields: [changed_by_id], references: [id], onUpdate: NoAction, map: "status_histories_changed_by_id_foreign")

  @@index([changed_by_id], map: "status_histories_changed_by_id_foreign")
  @@index([owner_type, owner_id, changed_at], map: "status_histories_owner_idx")
}

model time_workes {
  id            BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  company_id    BigInt    @db.UnsignedBigInt
  departemen_id BigInt    @db.UnsignedBigInt
  name          String    @db.VarChar(255)
  in            DateTime  @db.Time(0)
  out           DateTime  @db.Time(0)
  created_at    DateTime? @db.Timestamp(0)
  updated_at    DateTime? @db.Timestamp(0)
}

model user_address {
  id                  BigInt                     @id @default(autoincrement()) @db.UnsignedBigInt
  user_id             BigInt                     @db.UnsignedBigInt
  identity_type       user_address_identity_type @default(ktp)
  identity_numbers    String                     @db.VarChar(100)
  province            String                     @db.VarChar(100)
  city                String                     @db.VarChar(100)
  citizen_address     String                     @db.LongText
  residential_address String                     @db.LongText
  created_at          DateTime?                  @db.Timestamp(0)
  updated_at          DateTime?                  @db.Timestamp(0)
}

model user_attendances {
  id                        BigInt                      @id @default(autoincrement()) @db.UnsignedBigInt
  user_id                   BigInt                      @db.UnsignedBigInt
  user_timework_schedule_id BigInt?                     @db.UnsignedBigInt
  time_in                   DateTime?                   @db.Time(0)
  time_out                  DateTime?                   @db.Time(0)
  type_in                   user_attendances_type_in?
  type_out                  user_attendances_type_out?
  lat_in                    String?                     @db.VarChar(100)
  lat_out                   String?                     @db.VarChar(100)
  long_in                   String?                     @db.VarChar(100)
  long_out                  String?                     @db.VarChar(100)
  image_in                  String?                     @db.VarChar(255)
  image_out                 String?                     @db.VarChar(255)
  status_in                 user_attendances_status_in  @default(normal)
  status_out                user_attendances_status_out @default(normal)
  date_presence             DateTime?                   @db.Date
  created_by                BigInt?                     @db.UnsignedBigInt
  updated_by                BigInt?                     @db.UnsignedBigInt
  created_at                DateTime?                   @db.DateTime(0)
  updated_at                DateTime?                   @db.Timestamp(0)
}

model user_details {
  id             BigInt                       @id @default(autoincrement()) @db.UnsignedBigInt
  user_id        BigInt                       @db.UnsignedBigInt
  phone          String                       @db.VarChar(50)
  placebirth     String                       @db.VarChar(100)
  datebirth      DateTime                     @db.Date
  gender         user_details_gender          @default(m)
  blood          user_details_blood?
  marital_status user_details_marital_status?
  religion       user_details_religion?
  created_at     DateTime?                    @db.Timestamp(0)
  updated_at     DateTime?                    @db.Timestamp(0)
}

model user_employes {
  id                  BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id             BigInt    @unique(map: "ux_user_employes_user_id") @db.UnsignedBigInt
  departement_id      BigInt?   @db.UnsignedBigInt
  job_position_id     BigInt?   @db.UnsignedBigInt
  job_level_id        BigInt?   @db.UnsignedBigInt
  approval_line_id    BigInt?   @db.UnsignedBigInt
  approval_manager_id BigInt?   @db.UnsignedBigInt
  join_date           DateTime  @db.Date
  sign_date           DateTime  @db.Date
  resign_date         DateTime? @db.Date
  bank_name           String?   @db.VarChar(255)
  bank_number         String?   @db.VarChar(255)
  bank_holder         String?   @db.VarChar(255)
  saldo_cuti          Int?
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  user                users     @relation("UserEmployeOneToOne", fields: [user_id], references: [id], onDelete: Cascade, map: "fk_user_employes_user")
}

model user_families {
  id             BigInt                        @id @default(autoincrement()) @db.UnsignedBigInt
  user_id        BigInt                        @db.UnsignedBigInt
  fullname       String                        @db.VarChar(100)
  relationship   user_families_relationship    @default(wife)
  birthdate      DateTime                      @db.Date
  marital_status user_families_marital_status?
  job            String                        @db.VarChar(100)
  created_at     DateTime?                     @db.Timestamp(0)
  updated_at     DateTime?                     @db.Timestamp(0)
}

model user_formal_educations {
  id            BigInt                        @id @default(autoincrement()) @db.UnsignedBigInt
  user_id       BigInt                        @db.UnsignedBigInt
  institution   String                        @db.VarChar(100)
  majors        String                        @db.VarChar(100)
  score         Decimal                       @default(0.00) @db.Decimal(4, 2)
  start         Int?                          @db.Year
  finish        Int?                          @db.Year
  status        user_formal_educations_status
  certification Boolean                       @default(true)
  created_at    DateTime?                     @db.Timestamp(0)
  updated_at    DateTime?                     @db.Timestamp(0)
}

model user_informal_educations {
  id            BigInt                          @id @default(autoincrement()) @db.UnsignedBigInt
  user_id       BigInt                          @db.UnsignedBigInt
  institution   String                          @db.VarChar(100)
  start         Int?                            @db.Year
  finish        Int?                            @db.Year
  type          user_informal_educations_type   @default(day)
  duration      Int                             @default(1)
  status        user_informal_educations_status
  certification Boolean                         @default(true)
  created_at    DateTime?                       @db.Timestamp(0)
  updated_at    DateTime?                       @db.Timestamp(0)
}

model user_salaries {
  id           BigInt                     @id @default(autoincrement()) @db.UnsignedBigInt
  user_id      BigInt                     @db.UnsignedBigInt
  basic_salary Decimal                    @default(0.00) @db.Decimal(20, 2)
  payment_type user_salaries_payment_type @default(Monthly)
  created_at   DateTime?                  @db.Timestamp(0)
  updated_at   DateTime?                  @db.Timestamp(0)
}

model user_timework_schedules {
  id           BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id      BigInt    @db.UnsignedBigInt
  time_work_id BigInt    @db.UnsignedBigInt
  work_day     DateTime  @db.Date
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)

  @@unique([user_id, work_day], map: "uts_user_workday_unique")
}

model user_work_experiences {
  id            BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id       BigInt    @db.UnsignedBigInt
  company_name  String    @db.VarChar(100)
  start         Int?      @db.Year
  finish        Int?      @db.Year
  position      String?   @db.VarChar(100)
  certification Boolean   @default(true)
  created_at    DateTime? @db.Timestamp(0)
  updated_at    DateTime? @db.Timestamp(0)
}

model users {
  id                                                        BigInt                 @id @default(autoincrement()) @db.UnsignedBigInt
  company_id                                                BigInt                 @db.UnsignedBigInt
  name                                                      String                 @db.VarChar(255)
  nip                                                       String                 @db.VarChar(255)
  email                                                     String                 @db.VarChar(255)
  email_verified_at                                         DateTime?              @db.Timestamp(0)
  password                                                  String                 @db.VarChar(255)
  avatar                                                    String?                @db.VarChar(255)
  status                                                    users_status           @default(active)
  remember_token                                            String?                @db.VarChar(100)
  device_id                                                 String?                @db.VarChar(255)
  is_attendance                                             Boolean                @default(false)
  created_at                                                DateTime?              @db.Timestamp(0)
  updated_at                                                DateTime?              @db.Timestamp(0)
  deleted_at                                                DateTime?              @db.Timestamp(0)
  activity_logs                                             activity_logs[]
  attachments                                               attachments[]
  design_approvals                                          design_approvals[]
  design_requests_design_requests_acknowledged_by_idTousers design_requests[]      @relation("design_requests_acknowledged_by_idTousers")
  design_requests_design_requests_pic_idTousers             design_requests[]      @relation("design_requests_pic_idTousers")
  design_requests_design_requests_submitted_to_idTousers    design_requests[]      @relation("design_requests_submitted_to_idTousers")
  log_user_attendances                                      log_user_attendances[]
  status_histories                                          status_histories[]
  user_employe                                              user_employes?         @relation("UserEmployeOneToOne")
  wo_clearances                                             wo_clearances[]
  wo_services                                               wo_services[]
  wo_signoffs_wo_signoffs_done_by_idTousers                 wo_signoffs[]          @relation("wo_signoffs_done_by_idTousers")
  wo_signoffs_wo_signoffs_head_maintenance_idTousers        wo_signoffs[]          @relation("wo_signoffs_head_maintenance_idTousers")
  wo_signoffs_wo_signoffs_requester_verify_idTousers        wo_signoffs[]          @relation("wo_signoffs_requester_verify_idTousers")
  work_orders                                               work_orders[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model wo_clearances {
  id                    BigInt      @id @default(autoincrement()) @db.UnsignedBigInt
  work_order_id         BigInt      @db.UnsignedBigInt
  hygiene_clearance     Boolean?
  maintenance_clearance Boolean?
  verified_by_id        BigInt?     @db.UnsignedBigInt
  verified_at           DateTime?   @db.Timestamp(0)
  created_at            DateTime?   @db.Timestamp(0)
  updated_at            DateTime?   @db.Timestamp(0)
  users                 users?      @relation(fields: [verified_by_id], references: [id], onUpdate: NoAction, map: "wo_clearances_verified_by_id_foreign")
  work_orders           work_orders @relation(fields: [work_order_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "wo_clearances_work_order_id_foreign")

  @@index([verified_by_id], map: "wo_clearances_verified_by_id_foreign")
  @@index([work_order_id], map: "wo_clearances_work_order_id_foreign")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model wo_services {
  id            BigInt      @id @default(autoincrement()) @db.UnsignedBigInt
  work_order_id BigInt      @db.UnsignedBigInt
  description   String      @db.Text
  created_by_id BigInt?     @db.UnsignedBigInt
  created_at    DateTime    @default(now()) @db.Timestamp(0)
  users         users?      @relation(fields: [created_by_id], references: [id], onUpdate: NoAction, map: "wo_services_created_by_id_foreign")
  work_orders   work_orders @relation(fields: [work_order_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "wo_services_work_order_id_foreign")

  @@index([created_by_id], map: "wo_services_created_by_id_foreign")
  @@index([work_order_id], map: "wo_services_work_order_id_foreign")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model wo_signoffs {
  id                                           BigInt      @id @default(autoincrement()) @db.UnsignedBigInt
  work_order_id                                BigInt      @db.UnsignedBigInt
  done_by_id                                   BigInt?     @db.UnsignedBigInt
  head_maintenance_id                          BigInt?     @db.UnsignedBigInt
  requester_verify_id                          BigInt?     @db.UnsignedBigInt
  notes                                        String?     @db.Text
  signed_at                                    DateTime?   @db.Timestamp(0)
  created_at                                   DateTime?   @db.Timestamp(0)
  updated_at                                   DateTime?   @db.Timestamp(0)
  users_wo_signoffs_done_by_idTousers          users?      @relation("wo_signoffs_done_by_idTousers", fields: [done_by_id], references: [id], onUpdate: NoAction, map: "wo_signoffs_done_by_id_foreign")
  users_wo_signoffs_head_maintenance_idTousers users?      @relation("wo_signoffs_head_maintenance_idTousers", fields: [head_maintenance_id], references: [id], onUpdate: NoAction, map: "wo_signoffs_head_maintenance_id_foreign")
  users_wo_signoffs_requester_verify_idTousers users?      @relation("wo_signoffs_requester_verify_idTousers", fields: [requester_verify_id], references: [id], onUpdate: NoAction, map: "wo_signoffs_requester_verify_id_foreign")
  work_orders                                  work_orders @relation(fields: [work_order_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "wo_signoffs_work_order_id_foreign")

  @@index([done_by_id], map: "wo_signoffs_done_by_id_foreign")
  @@index([head_maintenance_id], map: "wo_signoffs_head_maintenance_id_foreign")
  @@index([requester_verify_id], map: "wo_signoffs_requester_verify_id_foreign")
  @@index([work_order_id], map: "wo_signoffs_work_order_id_foreign")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model wo_spareparts {
  id            BigInt      @id @default(autoincrement()) @db.UnsignedBigInt
  work_order_id BigInt      @db.UnsignedBigInt
  part_name     String      @db.VarChar(150)
  quantity      Decimal     @default(1.00) @db.Decimal(12, 2)
  remarks       String?     @db.Text
  created_at    DateTime?   @db.Timestamp(0)
  updated_at    DateTime?   @db.Timestamp(0)
  work_orders   work_orders @relation(fields: [work_order_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "wo_spareparts_work_order_id_foreign")

  @@index([work_order_id, part_name], map: "wo_spareparts_idx")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model work_orders {
  id                                                         BigInt             @id @default(autoincrement()) @db.UnsignedBigInt
  wo_no                                                      String?            @unique(map: "work_orders_wo_no_unique") @db.VarChar(50)
  requested_by_id                                            BigInt             @db.UnsignedBigInt
  request_date                                               DateTime           @db.Date
  department_provides                                        BigInt?            @db.UnsignedBigInt
  department_id                                              BigInt?            @db.UnsignedBigInt
  area                                                       String?            @db.VarChar(150)
  complaint                                                  String?            @db.Text
  asset_info                                                 String?            @db.VarChar(255)
  status                                                     work_orders_status @default(OPEN)
  start_time                                                 DateTime?          @db.Timestamp(0)
  end_time                                                   DateTime?          @db.Timestamp(0)
  created_at                                                 DateTime?          @db.Timestamp(0)
  updated_at                                                 DateTime?          @db.Timestamp(0)
  wo_clearances                                              wo_clearances[]
  wo_services                                                wo_services[]
  wo_signoffs                                                wo_signoffs[]
  wo_spareparts                                              wo_spareparts[]
  departements_work_orders_department_idTodepartements       departements?      @relation("work_orders_department_idTodepartements", fields: [department_id], references: [id], onUpdate: NoAction, map: "work_orders_department_id_foreign")
  departements_work_orders_department_providesTodepartements departements?      @relation("work_orders_department_providesTodepartements", fields: [department_provides], references: [id], onUpdate: NoAction, map: "work_orders_department_provides_foreign")
  users                                                      users              @relation(fields: [requested_by_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "work_orders_requested_by_id_foreign")

  @@index([department_id], map: "work_orders_department_id_foreign")
  @@index([department_provides], map: "work_orders_department_provides_foreign")
  @@index([requested_by_id], map: "work_orders_requested_by_id_foreign")
  @@index([status, request_date], map: "work_orders_status_date_idx")
}

enum qr_presences_type {
  in
  out
}

enum log_user_attendances_type {
  in
  out
}

enum user_address_identity_type {
  ktp
  sim
  passport
}

enum design_approvals_status {
  PENDING
  APPROVED
  REJECTED
}

enum user_families_relationship {
  wife
  husband
  mother
  father
  brother
  sister
  child
}

enum user_salaries_payment_type {
  Monthly
  Weekly
  Daily
}

enum design_requests_priority {
  high
  medium
  low
}

enum permit_approves_user_approve {
  w
  n
  y
}

enum user_attendances_type_in {
  qrcode
  face_device      @map("face-device")
  face_geolocation @map("face-geolocation")
}

enum user_details_gender {
  m
  w
}

enum user_families_marital_status {
  single
  married
  widow
  widower
}

enum user_informal_educations_type {
  day
  year
  month
}

enum bug_reports_platform {
  web
  android
  ios
}

enum user_attendances_type_out {
  qrcode
  face_device      @map("face-device")
  face_geolocation @map("face-geolocation")
}

enum user_details_blood {
  a
  b
  o
  ab
}

enum user_details_marital_status {
  single
  married
  widow
  widower
}

enum user_formal_educations_status {
  passed
  not_passed  @map("not-passed")
  in_progress @map("in-progress")
}

enum user_informal_educations_status {
  passed
  not_passed  @map("not-passed")
  in_progress @map("in-progress")
}

enum user_details_religion {
  islam
  protestan
  khatolik
  hindu
  buddha
  khonghucu
}

enum users_status {
  active
  inactive
  resign
}

enum design_requests_status {
  DRAFT
  SUBMITTED
  APPROVED
  REJECTED
  IN_PROGRESS
  DONE
  CANCELED
}

enum work_orders_status {
  OPEN
  IN_PROGRESS
  ON_HOLD
  DONE
  CANCELED
}

enum user_attendances_status_in {
  late
  unlate
  normal
}

enum user_attendances_status_out {
  late
  unlate
  normal
}
